---
const { title, path, show } = Astro.props;
---
<div class={`flex flex-col items-start gap-1 ${show ? '' : 'hidden'}`}>
  <h1 class="text-white text-32 font-bold">{title}</h1>
  <p class="text-white text-sm">{path}</p>
</div>